<h1> Registration Page </h1>
<% if flash.alert %>
   <div> <%= flash.alert %> </div>
<% end %>
<% if flash.notice %>
   <div> <%= flash.notice %> </div>
<% end %>
<% if flash[:error] %>
   <div> <%= flash[:error] %> </div>
<% end %>
<%= form_for(@user, :url => {:action => :create}) do |form| %>
   <table>
      <tr>
         <td class="side_attribute"><%= form.label(:first_name, "First name:")%></td>
         <td><%= form.text_field(:first_name) %> </td>
      </tr>
      <tr>
         <td class="side_attribute"><%= form.label(:last_name, "Last name:")%></td>
         <td><%= form.text_field(:last_name) %></td>
      </tr>
      <tr>
         <td class="side_attribute"><%= form.label(:login, "Login ID:")%></td>
         <td><%= form.text_field(:login) %></td>
      </tr>
      <tr>
         <td class="side_attribute"><%= form.label(:password, "Password:")%></td>
         <td><%= form.password_field(:password) %></td>
      </tr>
      <tr>
         <td class="side_attribute"><%= form.label(:password_confirmation, "Confirm password:")%></td>
         <td><%= form.password_field(:password_confirmation) %></td>
      </tr>
   </table>
   <p> To help us provide better movie suggestions, indicate your movie interests below. <br/>
   <%= form.label(:interests, "Enter movie genres that you're interested in:")%>
   <br/><%= form.text_field(:interests) %> </br></p>
   
   <div>As you know, one way to buy movies is by accumulating points by watching advertisements. <br/> If you'd like, type in any broad interests you have so that we can filter your ads appropriately. <br/>
   <%= form.label(:ad_interests, "Enter your interests for ad filtering:")%>
   <br/><%= form.text_field(:ad_interests) %> </div>
   
   <br/><div> <%= form.submit "Register" %> </div>
<% end %>